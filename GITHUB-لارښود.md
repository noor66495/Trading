# 📦 د GitHub لارښود — د نور احمد ټرېډر پلیټ فارم

## 🎯 موخه

ستاسو پلیټ فارم په **GitHub** کې واچوئ، ترڅو:
- فایلونه خوندي او منظم وي
- **ژوندی لینک** ترلاسه کړئ (GitHub Pages) — لکه: `https://ستاسو-نوم.github.io/noor-ahmad-trader/`

---

## ۱) چمتو شوي فایلونه

د **`noor-ahmad-trader-GitHub.zip`** فایل تیار کړی مې — ډاونلوډ یې کړئ او خلاص (Extract) کړئ. دننه یې دا دي:

| فایل / فولډر | توضیح |
|---|---|
| **index.html** | ⭐ بشپړ پلیټ فارم (۱۶۴KB) — دا یوازې کافي دی چې سایټ کار وکړي |
| **src/** (۷ فایلونه) | سرچینې — د راتلونکو بدلونونو لپاره |
| **build.py** | د جوړولو سکریپټ (سرچینې → index.html) |
| **README.md** | د پروژې لارښود کتابچه |
| **screenshots/** | د پلیټ فارم انځورونه |
| **.gitignore** | هغه فایلونه چې پورته کېږي نه (خپلکاره) |

❌ په زپ کې **نشته**: `node_modules`، `test/`، د لارښود docx — دوی د GitHub لپاره اړین نه دي.

---

## ۲) شپږ مرحلې — د فایلونو ترتیب (یوه یوه مرحله)

که غواړئ فایلونه **مرحله په مرحله** پورته کړئ، دا ترتیب تعقیب کړئ:

| مرحله | نوم | فایلونه |
|---|---|---|
| ۱ | 🎨 ډیزاین او بڼه | `src/style.css` |
| ۲ | 🌐 ژبې (پښتو / انګلیسي) | `src/i18n.js` |
| ۳ | ⚙️ د بازار انجن (سیګنالونه، ICT) | `src/sim.js` |
| ۴ | 📊 چارټ او AI چټ | `src/chart.js` + `src/chat.js` |
| ۵ | 🖥️ پاڼې او UI (۱۰ پاڼې، مینو، ساعت) | `src/pages.js` + `src/ui.js` |
| ۶ | 🚀 بشپړ فایل + اسناد | `index.html` + `build.py` + `README.md` + `screenshots/` + `.gitignore` |

> ⚠️ **مهم**: ټول فایلونه یو بل سره تړلي دي. کله چې **ټولې ۶ مرحلې** پورته شي، هغه وخت سایټ بشپړ کار کوي. (که یوازې `index.html` پورته کړئ هم سایټ کار کوي — خو راتلونکي بدلونونه اسانه نه وي.)
>
> فایلونه باید د ریپو په **سر (root)** کې واچوئ — نه په اضافي فولډر کې.

---

## ۳) لاره ۱ — اسانه (له براوزره، پرته له کمانډه)

1. **GitHub.com** ته لاړ شئ او حساب جوړ کړئ (که نلرئ — وړیا دی)
2. د پورته ښي خوا **+** تڼۍ → **New repository**
3. نوم ولیکئ: `noor-ahmad-trader` → **Public** غوره کړئ → **Create repository**
4. په نوي پاڼه کې: د "**uploading an existing file**" لینک باندې کلیک وکړئ
5. د زپ فایل خلاص کړئ — دننه ټول شیان (فایلونه + `src` فولډر) په هغه ساحه کې **کش کړئ (Drag & Drop)**
   - یا یوه یوه مرحله — د پورته جدول له مخې — پورته کړئ
6. لاندې **Commit changes** تڼۍ ووهئ

---

## ۴) ژوندی لینک فعالول (GitHub Pages) — مهم ⭐

1. د ریپو پاڼې ته لاړ شئ → **Settings** (ښي خوا)
2. ښکته مینو کې: **Pages**
3. **Source**: `Deploy from a branch` → **Branch**: `main` / `(root)` → **Save**
4. ۱–۲ دقیقې انتظار وکړئ — بیا ستاسو لینک:

```
https://ستاسو-نوم.github.io/noor-ahmad-trader/
```

---

## ۵) لاره ۲ — مسلکي (د git کمانډونو سره)

که په کمپیوټر کې **git** نصب لرئ (Terminal / Command Prompt):

```bash
cd noor-ahmad-trader

git init
git add .
git commit -m "Noor Ahmad Trader - AI Trading Platform"
git branch -M main
git remote add origin https://github.com/ستاسو-نوم/noor-ahmad-trader.git
git push -u origin main
```

(کچه/پاسورډ به یې پخپله وغواړي — انټر کړئ)

---

## ۶) راتلونکي بدلونونه (هر ځل چې غواړئ)

1. په `src/` کې بدلون راولئ (یا ما ته ووایاست چې بدلونونه وکړم)
2. کمانډ: `python3 build.py` — `index.html` تازه کېږي
3. پورته یې کړئ:
   ```bash
   git add .
   git commit -m "تازه بدلون"
   git push
   ```
   (یا په GitHub ویب پاڼه کې مستقیم فایل بدل کړئ)

---

## ۷) یادونه

- `index.html` پرته له انټرنیټه هم کار کوي — اوسمهال ډاټا یې **نمونه (Demo)** ده
- د ریښتیني بازار ډاټا (وړیا API) لپاره په `README.md` کې وګورئ
- که کومه ستونزه درته راغله یا غواړئ چې زه یې پخپله ډېر ساده کړم — ووایاست! 🤝
